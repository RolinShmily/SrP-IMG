# 安全策略

<p align="center">
  <a href="./SECURITY.md">English</a> • <b>简体中文</b>
</p>

## 受支持的版本

本项目未发布任何版本标签，仅维护 `main` 分支，部署工作流也仅发布该分支的内容。
修复会直接合入 `main`，请在报告前先基于最新的 `main` 复现。

## 报告漏洞

**请勿为安全问题提交公开 Issue。** 请通过邮件私下报告：

**<rol1n@srprolin.top>**

请尽量附上：

* 问题描述及你判断的影响范围；
* 受影响的文件或接口，包含完整的 URL / 请求 / 载荷；
* 复现步骤或最小化 PoC；
* 你的环境（浏览器、Node 版本、部署方式）；
* 是否希望公开致谢，以及署名的形式。

## 响应预期

本项目为个人业余维护，时间线均为尽力而为：

| 阶段 | 目标 |
| :-- | :-- |
| 确认收到报告 | 3 天内 |
| 初步判定（受理 / 不予受理） | 7 天内 |
| 已受理问题完成修复并上线 | 通常 30 天内 |

请在公开细节前给予合理窗口期 —— **90 天，或直到修复上线**，以先到者为准。
修复上线后我会告知你，若你希望署名也会在说明中致谢。

## 属于受理范围

* 跳转接口（`/pic`、`/api/pic`）及其对用户输入的处理。
* 静态画廊前端（`app/`、`components/`）。
* 构建脚本（`gen_img.py`）与 `.github/workflows/` 中的部署工作流。
* 仓库或其历史中泄露的密钥与凭据。
* 在本代码库中确实可被触达的依赖漏洞。

## 不属于受理范围

* **图片版权 / 授权投诉**：这并非安全问题，请参见 [README_zh](./README_zh.md#-开源协议) 的协议章节。
* **公开接口的滥用**（盗链、高并发请求）：该接口按设计即为公开、无鉴权、仅提供非敏感图片。
* **运营者自有域名上的加固缺失**：HSTS、WAF 规则、限流等均在 Cloudflare 账户层面配置，不属于本仓库范畴。
* Cloudflare 平台、Next.js 或其他上游依赖自身的漏洞 —— 请向上游报告。
* 依赖设备被入侵、恶意浏览器扩展或物理接触才能成立的问题。
* 没有实际影响证明的自动化扫描器输出。

## 测试准则

* 仅对你**自有**的部署进行测试，或对公开实例做只读检查。禁止破坏性测试、拒绝服务流量，或试图访问他人数据。
* 报告中不要包含真实凭据或第三方个人数据。
* 感谢你 —— 负责任的漏洞报告非常可贵。
